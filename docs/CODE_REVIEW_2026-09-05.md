# 코드 점검 및 수정 기록 — 2026-09-05

## 기준과 범위

- 기준 커밋: `f954ae7585f8d6d3f3dca177eee5b60bf23fac89`.
- 정상 로컬 저장소: `/Users/mindw/Projects/Dday`.
- 현재 앱 구조, 문서, 공통 Swift 로직, macOS 메뉴바, iOS 모델·알림·위젯 연계, Android 데이터 처리 및 빌드 스크립트를 점검했습니다.
- `/Users/mindw/Documents/Projects/Dday`에는 iCloud `dataless` 파일과 Git 조회 지연이 남아 있어 검증에 사용하지 않았습니다. 정상 저장소의 별도 임시 복제본에서 수정·빌드를 진행했습니다.

## 수정 내용

| 영역 | 확인한 문제 | 수정 |
| --- | --- | --- |
| 날짜 계산 | Swift가 `2026-extra-05-27`, `12:bad:30`처럼 잘못된 구성요소를 제거해 정상 날짜로 처리 | 날짜 `yyyy-MM-dd`, 시간 `HH:mm`과 실제 달력 유효성을 검증. Android에도 같은 형식 적용 |
| 데이터 업데이트 | JSON 디코딩만 성공하면 중복 ID나 계산 불가능한 날짜가 있는 목록도 정상 캐시를 대체 | 학회·마감 ID, 빈 마감 목록, 날짜·시간·타임존을 저장 전에 검증. 잘못된 기존 캐시는 번들 데이터로 복구 |
| 데이터 스키마 | 시간 정규식이 `24:00`부터 `29:59`까지 허용 | `00:00`–`23:59` 범위로 수정 |
| macOS 선택 | 사용자 D-Day 자동 선택 시 실제 UUID 대신 공통 문자열 `deadline`을 저장. 모든 마감이 지난 경우 선택을 저장하지 않음 | 선택·복원·체크 표시·삭제에 동일한 실제 ID를 사용하고 지난 마감의 기본 선택도 저장 |
| macOS 오류·빈 목록 | 타이머가 시작 오류를 덮어쓰고, 빈 목록에서 사용자 D-Day를 추가할 수 없음 | 오류 상태 유지와 데이터 재시도·사용자 D-Day 추가 경로 제공 |
| macOS 메뉴 | 매분 전체 메뉴와 배지를 다시 만들고, 메뉴 자동 검증이 업데이트 중 비활성화를 덮어씀 | 메뉴를 열 때 구성하고 동일한 배지는 재사용. 업데이트 상태를 메뉴 검증에 반영 |
| iOS 알림 | 예약 당시 D-Day가 실제 전달일에도 남고, 예약 도중 해제해도 이전 알림이 다시 등록될 수 있음 | 실제 알림 예정일 기준으로 문구 생성. 예약·삭제를 순서대로 처리하고 오래된 비동기 결과를 무시 |
| iOS 알림·복귀 | 많은 마감의 예약 순서가 가까운 알림을 우선하지 않음. 앱 복귀 시 알림·위젯 갱신 부족 | 다른 알림을 보존하며 가까운 알림부터 앱의 보수적 64개 예산 안에서 예약. 앱 활성화 시 상태 갱신 |
| iOS 복구 | 학회 로딩 실패가 저장된 사용자 D-Day도 숨기며, 이후 업데이트 성공 후에도 오류 화면 유지 | 사용자 마감을 독립적으로 읽고 성공 시 오류를 해제. 해당 날짜의 서머타임 약어 표시도 수정 |
| 패키징 | 초기·후반 실패 시 임시 패키징 디렉터리가 남고, 새 작업공간에서는 공증 ZIP의 상위 폴더가 없을 수 있음 | 정리 트랩을 작업 초반부터 끝까지 유지하고 ZIP 상위 폴더 생성 |

빈 최상위 카탈로그, 누락/null 마감 시간의 23:59 기본값, AoE UTC-12, 알 수 없는 분류·마감 종류의 기존 fallback 동작은 검증에 포함했습니다. 학회 일정 자체는 변경하지 않았습니다.

## 검증

- 기존 `DdayCoreChecks` 통과 후 잘못된 날짜의 회귀 체크를 추가했고, 수정 전 `malformed date was accepted: 2026-extra-05-27` 실패를 재현했습니다. 수정 후 전체 공통 검증이 통과했습니다.
- `swift build` 및 `./scripts/build_app.sh` 통과. 생성된 앱의 Info.plist, 서명 무결성, 번들 학회 데이터 일치를 확인했습니다.
- iPhone/iPad 앱과 WidgetKit 확장을 `generic/platform=iOS Simulator`, `CODE_SIGNING_ALLOWED=NO`로 빌드했습니다.
- Android `:app:testDebugUnitTest` 15개 및 `:app:assembleDebug` 통과. 실제 공용 데이터의 34개 학회·196개 마감을 파싱했습니다.
- 모바일 모델·알림 회귀 체크 10개가 `bash Apps/Mobile/Checks/run.sh`로 통과했습니다. 기존 GitHub Actions에 이 명령을 추가했습니다.
- macOS 메뉴 동작 회귀 체크 18개가 `./scripts/check_macos_menu.sh`로 통과했습니다. `Checks/DdayAppChecks/README.md`에 실행 조건을 기록했으며 로그인된 macOS 그래픽 세션이 필요합니다.
- 데이터 스키마 시간 경계, 변경한 셸 스크립트 문법, `git diff --check`를 확인했습니다.

## 검증 범위의 한계

이번 검증은 로컬 빌드, 코드 검토, 격리된 설정·모의 알림 센터를 사용하는 동작 체크입니다. 실제 iPhone/Android 기기의 알림 전달, 홈·잠금화면 위젯 갱신, 앱 전체 화면 조작, 백그라운드 동작은 별도 기기 검증이 필요합니다. 성능 개선은 반복 작업 제거를 확인한 것이며 CPU·배터리 수치를 실측하지 않았습니다.

앱 설치·공증 제출·스토어 업로드·원격 푸시는 실행하지 않았으며 GitHub Actions의 새 원격 실행 결과는 아직 없습니다. Android release AAB와 실제 배포용 서명도 이번 검증에 포함하지 않았습니다.
